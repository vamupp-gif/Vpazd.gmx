<!--This Document is generated by GameMaker, if you edit it by hand then you do so at your own risk!-->
<object>
  <spriteName>spr_madar_idle</spriteName>
  <solid>0</solid>
  <visible>-1</visible>
  <depth>0</depth>
  <persistent>0</persistent>
  <parentName>&lt;undefined&gt;</parentName>
  <maskName>&lt;undefined&gt;</maskName>
  <events>
    <event eventtype="0" enumb="0">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>// --- obj_madar: Create Event ---

// Állapot: "idle" (várakozik) vagy "fly" (repül)
state = "idle";

// Sebességek
hspd = 0;
vspd = 0;

// Időzítők
fly_timer = irandom_range(120, 240); // kb. 2–4 másodperc múlva repülni kezd
egg_timer = irandom_range(60, 180);  // tojásdobás időzítő

// Repülés viselkedéshez
height_target = y;   // cél magasság (repülés közben módosul)
angle = 0;           // sin hullámhoz
amplitude = 4;       // lebegés mértéke
</string>
          </argument>
        </arguments>
      </action>
    </event>
    <event eventtype="3" enumb="0">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>// --- obj_madar: Step Event ---

switch (state) {
  // -------------------------
  // 🕊️ IDLE (várakozik)
  // -------------------------
  case "idle":
    sprite_index = spr_madar_idle;
    image_speed = 0.2;

    // Véletlenszerű idő után elrepül
    fly_timer -= 1;
    if (fly_timer &lt;= 0) {
      state = "fly";
      sprite_index = spr_madar_fly;
      image_speed = 0.4;

      height_target = y - irandom_range(20, 120);  // 20–120 pixel magasra
      hspd = choose(-1, 1);                        // balra vagy jobbra repül
      fly_timer = irandom_range(90, 180);          // mennyi ideig repül
    }
    break;

  // -------------------------
  // 🐦 FLY (repül)
  // -------------------------
  case "fly":
    sprite_index = spr_madar_fly;
    image_speed = 0.4;

    upordown = irandom_range(0, 1);
    angle = 1;
    // Sin hullámos mozgás (kis lebegés)
    if (place_meeting(x, y + 8, obj_solid)) {
      y -= 5;
    } else if (!place_meeting(x, y + 8, obj_solid) &amp;&amp; upordown == 0) {
      y -= 5;
    } else if (!place_meeting(x, y + 8, obj_solid) &amp;&amp; upordown == 1) {
      y += 5;
    }
    // Finom magasság-korrekció
    if (y &gt; height_target + 5) y -= 0.5;
    if (y &lt; height_target - 5) y += 0.5;

    // Vízszintes mozgás
    x += hspd * 5;

    // Véletlenszerű tojásdobás
    egg_timer -= 1;
    if (egg_timer &lt;= 0) {
      instance_create(x, y + 8, obj_ojas);
      egg_timer = irandom_range(60, 800);
    }

    // -----------------------------fly_timer
    // Leszállás logika (javított)
    // -----------------------------+
   
    fly_timer -= 1;
    if (fly_timer &lt;= 0) {

      state = "ground";
      hspd = 1;
      image_speed = 0.2;
      fly_timer = irandom_range(120, 240);
      break;
    }
  case "ground":
      fly_timer -= 1;
    if (fly_timer &lt;= 0) {

    // Ha van alatta talaj
    if (place_meeting(x, y + 8, obj_solid)) {
      // Finoman lemozgatjuk, amíg pont a talaj fölé nem ér
      while (!place_meeting(x, y + 1, obj_solid)) {
        y += 1;
      }

      // Most biztosan talaj fölött van
      state = "idle";
      hspd = 0;
      image_speed = 0.2;
      fly_timer = irandom_range(120, 240);

      break;
    } else {
      // Ha nincs talaj, lassan ereszkedik lefelé
      y += 10;
    }
   }
   else
   {
    sprite_index = spr_madar_fly;
    image_speed = 0.4;

    upordown = irandom_range(0, 1);
    angle = 1;
    // Sin hullámos mozgás (kis lebegés)
    if (place_meeting(x, y + 8, obj_solid)) {
      y -= 5;
    } else if (!place_meeting(x, y + 8, obj_solid) &amp;&amp; upordown == 0) {
      y -= 5;
    } else if (!place_meeting(x, y + 8, obj_solid) &amp;&amp; upordown == 1) {
      y += 5;
    }
    // Finom magasság-korrekció
    if (y &gt; height_target + 5) y -= 0.5;
    if (y &lt; height_target - 5) y += 0.5;

    // Vízszintes mozgás
    x += hspd * 5;

    // Véletlenszerű tojásdobás
    egg_timer -= 1;
    if (egg_timer &lt;= 0) {
      instance_create(x, y + 8, obj_ojas);
      egg_timer = irandom_range(60, 800);
    }

   }
    // Nézzen a mozgás irányába
    image_xscale = sign(hspd);

    // Biztonsági ellenőrzés: ha valami miatt leesne a room aljára,
    // visszaemeljük
    if (y &gt; room_height - 16) {
      y = room_height - 32;

      state = "idle";
      sprite_index = spr_madar_idle;  // azonnali sprite váltás
      image_speed = 0.2;
      hspd = 0;

      fly_timer = irandom_range(120, 240);
    }
    break;
}
</string>
          </argument>
        </arguments>
      </action>
    </event>
    <event eventtype="4" ename="obj_bullet">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>// --- obj_madar: Collision with obj_bullet ---

// Madár felrepül, ha eltalálja a golyó
state = "fly";
sprite_index = spr_madar_fly;
image_speed = 0.4;
height_target = y - irandom_range(20, 40);
hspd = choose(-1, 1) * 1.5;  // gyorsabban elrepül

// (Ha akarod, a golyó is eltűnhet)
with (other) instance_destroy();
</string>
          </argument>
        </arguments>
      </action>
    </event>
  </events>
  <PhysicsObject>0</PhysicsObject>
  <PhysicsObjectSensor>0</PhysicsObjectSensor>
  <PhysicsObjectShape>0</PhysicsObjectShape>
  <PhysicsObjectDensity>0.5</PhysicsObjectDensity>
  <PhysicsObjectRestitution>0.100000001490116</PhysicsObjectRestitution>
  <PhysicsObjectGroup>0</PhysicsObjectGroup>
  <PhysicsObjectLinearDamping>0.100000001490116</PhysicsObjectLinearDamping>
  <PhysicsObjectAngularDamping>0.100000001490116</PhysicsObjectAngularDamping>
  <PhysicsObjectFriction>0.200000002980232</PhysicsObjectFriction>
  <PhysicsObjectAwake>-1</PhysicsObjectAwake>
  <PhysicsObjectKinematic>0</PhysicsObjectKinematic>
  <PhysicsShapePoints/>
</object>
